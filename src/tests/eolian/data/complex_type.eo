class Complex_Type {
   methods {
      @property a {
         set {
            return: own(list<array<own(Eo**)>*>*);
         }
         get {
         }
         values {
            own(list<int>*) value;
         }
      }
      foo {
         params {
            own(char*) buf;
         }
         return: own(list<Eina.Stringshare*>*); /*@ comment for method return */
      }
   }
}
