ssg_sources_scenegraph = []

subdir('core')
subdir('backend')

foreach file: ssg_sources_scenegraph_core
   ssg_sources_scenegraph += join_paths('scenegraph', file)
endforeach

foreach file: ssg_sources_scenegraph_backend
   ssg_sources_scenegraph += join_paths('scenegraph', file)
endforeach
