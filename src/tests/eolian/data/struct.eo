struct Named {
    field: int @by_ref;
    something: string;
}

struct Another {
    field: Named;
}

/* opaque struct */
struct Opaque;

struct @extern Not.Generated {
    field: int;
}

class Struct {
   [[Struct @since 1.66]]
   methods {
      foo {
         [[Foo docs. This is $monospace. This is alone-standing $.]]
         params {
            idx: int;
         }
         return: mstring @move;
      }
      bar {
         return: Named @by_ref;
      }
   }
}
