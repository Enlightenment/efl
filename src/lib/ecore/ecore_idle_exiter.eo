class Ecore_Idle_Exiter (Eo.Base)
{
   eo_prefix: ecore_idle_exiter;
   constructors {
      constructor {
         /*@ Contructor. */
         params {
            @in Ecore_Task_Cb func;
            @in const(void)* data;
         }
      }
   }
   implements {
      Eo.Base.constructor;
      Eo.Base.destructor;
   }
}