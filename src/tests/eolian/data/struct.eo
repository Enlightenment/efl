struct Named {
    field: int;
    something: const(char)*;
}

struct Another {
    field: Named;
}

/* opaque struct */
struct Opaque;

class Struct {
   methods {
      foo {
         params {
            idx: int;
         }
         return: own(char*);
      }
   }
}
