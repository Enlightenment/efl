install_subdir('modules',
  install_dir : join_paths(dir_data, 'elua')
)

install_subdir('core',
  install_dir : join_paths(dir_data, 'elua')
)

install_subdir('apps',
  install_dir : join_paths(dir_data, 'elua')
)
