interface Dummy.Test_Iface
{
   methods {
      emit_nonconflicted {
      }
      @property iface_prop {
         get {}
         set {}
         values {
            data: int;
         }
      }

      method_protected @protected @const {
         params {
            @in x: int;
         }
         return: int;
      }

      call_method_protected @const {
         params {
            @in x: int;
         }
         return: int;
      }

   }
   events {
      nonconflicted: void;
   }
}
