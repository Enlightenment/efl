class Ecore_Job (Eo.Base)
{
   eo_prefix: ecore_job;
   constructors {
      constructor {
         /*@ Contructor. */
         params {
            @in Ecore_Cb func;
            @in const(void)* data;
         }
      }
   }
   implements {
      Eo.Base.constructor;
      Eo.Base.destructor;
   }
}