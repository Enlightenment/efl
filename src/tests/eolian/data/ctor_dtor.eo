class Ctor_Dtor (Base) {
   methods {
      custom_constructor_1 @constructor {
         params {
            @in int a;
            @in char b;
         }
      }
      custom_constructor_2 @constructor {
      }
   }
   implements {
      class.constructor;
      class.destructor;
      Base.constructor;
      Base.destructor;
   }
}
