class Complex_Type {
   methods {
      @property a {
         set {
            return: own(list<array<own(Eo**)> >);
         }
         get {
         }
         values {
            value: own(list<int>);
         }
      }
      foo {
         params {
            buf: own(char*);
         }
         return: own(list<stringshare>); [[comment for method return]]
      }
   }
}
