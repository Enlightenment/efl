collections {
   styles {
      style { name: "tb_style";
         base: "font=Sans font_size=20 color=#fff";
      }
   }
   group { name: "test_text_cursor";
      parts {
         part { name: "text";
            type: TEXTBLOCK;
            entry_mode: EDITABLE;
            cursor_mode: BEFORE;
            description { state: "default" 0.0;
               min: 300 300;
               text {
                  style: "tb_style";
               }
            }
         }
      }
   }
}
